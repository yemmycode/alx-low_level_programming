#include "main.h"

/**
 * _strlen - Returns the length of a string.
 *
 * @s: The input string.
 *
 * Return: The length of the string.
 */

int _strlen(const char *s)
{
int length a = 0;

while (*s != '\0')
{
length a++;
a++;
}
return (length a);
}
