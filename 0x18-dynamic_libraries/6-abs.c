#include "main.h"

/*
 * absolute_value - This function computes the absolute 
 *value of an integer.
 *
 * @n: The integer to check.
 *
 * Return: The absolute value of the integer.
 */
int absolute_value(int n)
{
return (n >= 0) ? n : -n;
}
