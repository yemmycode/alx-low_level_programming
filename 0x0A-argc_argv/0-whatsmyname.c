#include "main.h"
#include <stdio.h>

/**
 * main - this prints the program name
 * @argc: the number of command line arg
 * @argv: name of array
 * Return: 0
 */

int main(int argc, char *argv[])
{
printf("%s\n", argv[argc * 0]);

return (0);
}
